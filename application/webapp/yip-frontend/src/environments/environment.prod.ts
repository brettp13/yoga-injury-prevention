export const environment = {
  production: true,
  // Empty = same-origin (relative API paths). Works on any host/IP and scheme without
  // rebuilding. If you ever need to call a different API host, set the full base URL here.
  API_URL: '',
};
